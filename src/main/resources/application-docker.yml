# Docker Profile (운영용 Docker 컨테이너)
# 개발용 docker 테스트가 필요하면 application-docker-dev.yml을 별도로 만들어 사용하세요.
# show-sql: false, ddl-auto: validate 는 운영 환경에서 필수입니다.

spring:
  datasource:
    url: jdbc:mysql://db:3306/vue_personal_project_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  servlet:
    multipart:
      max-file-size: '1GB'
      max-request-size: '5GB'

logging:
  level:
    org.hibernate.SQL: WARN
    org.springframework.web: INFO

server:
  tomcat:
    # max-http-form-post-size: x-www-form-urlencoded 본문 한도. multipart에는 적용되지 않음.
    # 기본 2MB로 두되 일부 폼 전송을 위해 10MB로 제한.
    max-http-form-post-size: 10MB

jwt:
  cookie-secure: ${JWT_COOKIE_SECURE:false}
  cookie-domain: ${JWT_COOKIE_DOMAIN:}

# CORS 허용 도메인 (운영 환경)
# TODO: HTTPS 인프라 정비 후 http:// 항목과 raw IP origin 제거 예정
# 환경변수로 재정의 가능: APP_CORS_ALLOWED_ORIGINS=도메인1,도메인2,...
app:
  cors:
    allowed-origins: "http://hyunchang.synology.me,https://hyunchang.synology.me,http://m.hyunchang.synology.me,https://m.hyunchang.synology.me,http://hyunchang.synology.me:3100,http://hyunchang.synology.me:3200,http://m.hyunchang.synology.me:3100,http://m.hyunchang.synology.me:3200,https://hyunchang.synology.me:3100,https://hyunchang.synology.me:3200,https://m.hyunchang.synology.me:3100,https://m.hyunchang.synology.me:3200,http://125.141.20.218:3100,http://125.141.20.218:3200"
