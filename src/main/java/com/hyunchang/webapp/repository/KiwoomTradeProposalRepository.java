package com.hyunchang.webapp.repository;
import com.hyunchang.webapp.entity.*; import java.time.LocalDateTime; import java.util.*; import org.springframework.data.jpa.repository.JpaRepository;
public interface KiwoomTradeProposalRepository extends JpaRepository<KiwoomTradeProposal,Long>{ List<KiwoomTradeProposal> findByRunIdInOrderByIdAsc(Collection<Long> ids); long countByActionInAndCreatedAtGreaterThanEqual(Collection<KiwoomTradeProposal.Action> actions, LocalDateTime at); boolean existsByStockCodeAndActionInAndCreatedAtGreaterThanEqual(String code, Collection<KiwoomTradeProposal.Action> actions, LocalDateTime at); }
